const paying = (operator, phone, money) => {
  console.log(operator, phone, money);
  return Math.floor(Math.random() * 2);
  // if ()) {
  //   
  // } else {
  //   s
  // };
}

export default paying;